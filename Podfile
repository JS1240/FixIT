# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FIXIT.CO' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FIXIT.CO

  target 'FIXIT.COTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FIXIT.COUITests' do
    # Pods for testing
  end

end
